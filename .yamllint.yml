# Shared yamllint config for GitHub workflows.
# Relaxed vs. yamllint defaults: longer lines, `on:` trigger key allowed,
# no mandatory `---` document start.
extends: default

rules:
  document-start: disable
  truthy:
    allowed-values: ["true", "false", "on"]
  line-length:
    max: 120
